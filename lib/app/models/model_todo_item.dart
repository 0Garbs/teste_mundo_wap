import 'model_todo.dart';

class ModelTodoItem {
  final bool done;
  final ModelTodo todo;
  // final List<ModelFilledField> filledFields;

  ModelTodoItem({required this.done, required this.todo});
}
